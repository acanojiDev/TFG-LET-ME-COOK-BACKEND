import { AuthClient } from '@supabase/auth-js'
export class AuthController{
    /**
     * TODO: Investigar si usar createAuthUser
     * @params email, password
     */
    const {data,error} = await supabase

}